# SAER-PIA Design

SAER keeps B0V variable-only activation matching as the continuous optimization backbone. Server-side suffix attention from H_obs is frozen into an edge graph and used only for gated discrete candidate reranking.
